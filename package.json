{
  "name": "squash-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Single-page squash tournament tracker.",
  "scripts": {
    "start": "serve . -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
